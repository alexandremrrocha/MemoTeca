import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.memoteca',
  appName: 'MemoTeca',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
